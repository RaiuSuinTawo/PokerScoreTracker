# 保证 shell 脚本无论在什么平台 checkout 都是 LF
# （避免 Windows 端 core.autocrlf 把 CRLF 带到 Linux 里，导致 bash 报 "bad interpreter"）

* text=auto eol=lf

*.sh       text eol=lf
*.bash     text eol=lf
*.template text eol=lf

# Windows 专属脚本可以保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
*.db  binary
